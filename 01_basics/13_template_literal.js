// old way
const naem = "Arjun1";
const msg = "Hello," + naem + "!";

// new way
const name = "Arjun";
const msg = `Hello ${name}`

// ecpression not just varibles
const price  = 645685;
const qty = 3;
const get = 0.18;

// Any expressiuon inside ${}
console.log(`items: ${qty}`);
console.log(`Total ${pri$ce*qty}`);
console.log(`which GST ${(price*qty* (1+ gst).toFirxed(2))}`);
console.log(`Discount? ${qty > 2 ? "Yes 5% off" : "No"}`);

// items : 3 total 
